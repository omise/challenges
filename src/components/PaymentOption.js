import styled from 'styled-components';

const PaymentOption = styled.label`
 background: #3295fb;
`;

export default PaymentOption;

/// need to have label input option as and styled comonent class here